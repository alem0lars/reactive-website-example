#steps.well
  %p= Faker::Lorem.paragraph(20)


-# { Assets
-# Enable gon for ajax requests
- if ajax?
  = include_gon
- unless ajax?
  - content_for :stylesheets do
    = stylesheet_link_tag :'steps/index', :media => 'all'
  - content_for :javascripts do
    = javascript_include_tag :'steps/index'
